*.s9pk
startos/*.js
node_modules/
.DS_Store
.vscode/
docker-images
javascript
ncc-cache
tasks/
bun.lock
# Downloaded service logs kept for local debugging — never committed
*-logs.html
*-logs.txt
*.log
# Written by `testing/injector.py enable`; a build carrying it ships an action
# that corrupts user data, so it must never be committable
/startos/actions/other/injectTestFaults.ts
